box: centos:6
build:
  steps:

  - script:
    name: hello world
    code: |
      echo 'Hello World'
      cat /etc/passwd
      pwd
      yum list
      sudo cat /etc/sudoers
      sudo yum -y update
