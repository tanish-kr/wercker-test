#box: centos:6
box: ippokm/AWS-Ansible@0.0.19
build:
  steps:

  - script:
    name: hello world
    code: |
      echo 'Hello World'
      cat /etc/passwd
      pwd
      yum list
      ls -la /etc
      sudo yum -y update
      yum search ruby
