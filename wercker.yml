#box: centos:6
services: centos-serverspec
build:
  steps:

  - script:
    name: hello world
    code: |
      echo 'Hello World'
      cat /etc/passwd
      pwd
      yum list
      ls -la /etc
      sudo yum -y update
      yum search ruby
